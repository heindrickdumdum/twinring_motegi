
<section class="bottom-slider">
  <div class="l-section-title">
    <%- include('/partials/heading', {
      modifier: 'heading-recommend',
      text: 'Recommendation',
      altSubtext: '',
      subtextvg: 'recommendation-title'
    }) %>
  </div>

  <div class="swiper-container slider-recommendation js-slider-bottom">
    <ul class="swiper-wrapper slider-wrapper">
      <% getData().data.recommend.forEach(item => { %>
      <li class="swiper-slide slider-item">
        <%- include('/partials/article-no-design', {
          category: '',
          image: item.image,
          heading: item.heading,
          type: '',
          date: '',
          link: '',
          modifier: 'article-recommendation',
          heading: item.heading,
          buttonLink: item.buttonLink,
          buttonText: item.buttonText
        }) %>
      </li>
      <% }) %>
    </ul>
  </div>
</section>
